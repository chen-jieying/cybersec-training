# Copilot Workspace Instructions

This workspace contains a Vue 3 + Element Plus frontend and a Spring Boot + MySQL backend for a junior high cybersecurity training platform.

- Frontend is in `frontend/`
- Backend is in `backend/`
- Use `npm install` and `npm run dev` to start the frontend
- Use Maven to build and run the backend
